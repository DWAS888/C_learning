*#include <stdio.h>

int main()
{
    int n, i;

    /* 1. 读取待判断的整数 n */
    scanf("%d", &n);

    /* 2. 先把“明显不是质数”的情况剔除：
          质数定义要求大于 1，所以 ≤1 直接输出 no 并结束程序 */
    if (n < 2) {
        printf("no");
        return 0;
    }

    /* 3. 核心判断：
          只需试除到 √n 即可，因为如果 n 有因子，那么一定有一个 ≤√n。
          令 i 从 2 开始，一直到 i*i 超过 n 为止。 */
    for (i = 2; i * i <= n; ++i) {
        if (n % i == 0) {          /* 一旦整除，说明 i 是 n 的一个真因子 */
            printf("no");          /* 不是质数 */
            return 0;              /* 立即结束，不再继续试除 */
        }
    }

    /* 4. 循环结束都没找到因子，说明 n 是质数 */
    printf("yes");
    return 0;
} 
	 */
	 

